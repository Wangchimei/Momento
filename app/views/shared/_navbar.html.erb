<div class="px-3 py-5 nav-bg">
  <nav class="navbar navbar-expand-md fixed-top navbar-dark justify-content-center">
    <%=link_to image_tag( "navbar_logo.png", class:"mx-3 my-1", size: "165x66"), feeds_path%>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsingNavbar"
    aria-controls="collapsingNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="navbar-collapse collapse" id="collapsingNavbar">
      <% if user_signed_in? %>
        <ul class="navbar-nav justify-content-center">
          <li class="nav-item">
            <%= link_to '新規投稿', new_feed_path, class: "nav-link #{ "active" if current_page?(new_feed_path)}" %>
          </li>
          <li class="nav-item">
            <%= link_to "プロフィール", user_path(current_user.id), class: "nav-link #{ "active" if current_page?(user_path(current_user.id))}" %>
          </li>
          <li class="nav-item">
            <%#= link_to "お気に入り一覧", favorite_path(current_user.id), class: "nav-link #{ "active" if current_page?(favorite_path(current_user.id))}" %>
          </li>
        </ul>
        <ul class="nav navbar-nav ml-auto justify-content-end">
            <li class="nav-item">
              <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "nav-link"%>
            </li>
        </ul>
      <% end %>
    </div>
  </nav>
</div>
